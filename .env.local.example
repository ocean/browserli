# Local Development Configuration

# Playwright Server (run: node playwright-server.js in another terminal)
PLAYWRIGHT_SERVER_URL=ws://127.0.0.1:3000

# API Keys (comma-separated) for local testing
API_KEYS=dev-key-local,dev-key-placemake,dev-key-test

# Optional: Set to 1 to use local Playwright server instead of Cloudflare
USE_LOCAL_PLAYWRIGHT=1
